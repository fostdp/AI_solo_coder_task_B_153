FROM python:3.12-slim-bookworm

WORKDIR /app

RUN pip install --no-cache-dir paho-mqtt==2.1.0

COPY simulator/spindle_simulator.py ./spindle_simulator.py

ENV PYTHONUNBUFFERED=1

ENTRYPOINT ["python", "spindle_simulator.py"]
CMD ["--host", "mqtt", "--port", "1883", "--interval", "60"]
